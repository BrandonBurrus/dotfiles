" Brandon Burrus .vimrc

" Set the color scheme to Neon Chalk
colorscheme neonchalk

" Load *.vim files from the runtime config dir
runtime! config/*.vim

" Enable syntax highlighting
syntax on

" Enable vim filetype detection, type-specific plugins, and auto-indentation
filetype plugin indent on

